import 'package:flutter/material.dart';

// Your brand's dark blue - used consistently across the whole app.
const Color brandDarkBlue = Color(0xFF002E52);

// Direct URL to your logo - no need to bundle it as an asset, since it's
// always available online (and this screen already requires internet
// the first time anyway).
const String logoUrl = 'https://cdn.800globalenglish.com/content/images/800_main_logo_dk.png';